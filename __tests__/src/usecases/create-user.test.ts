import { UserData } from '../../../src/usecases/create-user/user-data'

describe('Create user', () => {
  test('Return 400 if UserId is not provided', async () => {
    const users: UserData[] = []
    return users
  })
})
